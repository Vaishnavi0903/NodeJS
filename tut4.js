const colors = require('colors');
console.log("hello".red)
console.log("package.json".green);


//In this tut we have installed packages using :
// npm i colors;
// npm i simple-node-logger

// On running above commands package-lock.json and node module is automatically generated.
// package-lock.json stores the details of package.json
// Node modules contain the packages we have installed and the dependent packages.